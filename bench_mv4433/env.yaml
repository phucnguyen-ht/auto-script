# mv-4433 overrides (deep-merged on top of ../env.yaml).
backends:
  - name: vllm
  # - name: sglang
  #   serve_script: bench_mv4433/serve_sglang_ds3.2.sh
  #   model: deepseek

# Phase order (bench/profile are this ticket's; readable/eval are shared).
phases:
  - bench
  - profile
  - readable
  - eval

eval:
  datasets:
    repobench: { runs: 1 }
    mmlu:      { runs: 1 }
    gsm8k:     { runs: 1 }
