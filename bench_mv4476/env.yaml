# mv-4476 overrides (deep-merged on top of ../env.yaml).
backends:
  - name: vllm
  # - name: sglang
  #   serve_script: <ticket>/serve_sglang_<model>.sh
  #   model: deepseek

# Phase order (eval before readable).
phases:
  - eval
  - readable

eval:
  datasets:
    gsm8k:
      runs: 1
      method: script      # lm_eval (standard) | script (datasets/gsm8k/gsm8k.py)
